# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. engine, otap).
# Must be one of the components listed in rust/otap-dataflow/.chloggen/config.yaml.
component: otap

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: "S3 object storage gains `unsigned_payload`; `exporter:series_parquet` defaults it on for TLS endpoints, so uploads skip hashing every byte, and off for plain HTTP."

# Mandatory: One or more tracking issues related to the change. You can use the PR number here if no issue exists.
# PLACEHOLDER: no PR exists for this branch yet. Replace 4128 with the real PR number before merge.
issues: [4128]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries. Breaking entries must include a `Migration:`
# section with the specific action consumers must take. Keep notes to 200
# characters and subtext to 300 characters.
subtext: |
  Other exporters keep signed payloads. The default follows the endpoint the
  store uses, `AWS_ENDPOINT_URL` included, and yields to `AWS_UNSIGNED_PAYLOAD`.
  A store or bucket policy that refuses UNSIGNED-PAYLOAD answers 403 to every
  upload; set `unsigned_payload: false`.
